public class @ModuleManager extends @ClassLoadHelper implements Javium {


        public void onModuleEnable();
        .stream(listening, Modules);
        .streamline(doListen; -> Modules()
        
         <@PathImport>?\src\Modules>getClazz.requeste (return)
         clazz[clazzMapper]doReturn(true));

        if (class{clazz} =>> throws; *exception,
        || doAbord (true));

        else (doLoad) !>> (ClassLoadHelper.onClassLoad(class*));
        
        do (listen.onClassUnloading (doUnload) !>> (ClassUnloadHelper.onClassUnloading(class*)));

        return (class.listen); doListen{ClassLoadHelper, ClassUnloadHelper
        (atEntry; clazzMapper{clazz}doStream{@Export}true);

    }
}