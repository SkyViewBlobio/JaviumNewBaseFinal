private newBase;
private listener;

final class @ListenerClassLoad extends @ManagerListener
<ModuleManager>onClassLoad>> {


            @priorityOrder
            @import
            @ModuleManager
            @client

            public ListenerClassLoad(@ModuleManager manager)
            {
                super (listener,
                            onClassLoading.doAccess; >> clazz([clazzMapper]return);
                            Class.LOAD.CONSTRUCT,
                            @import; method(doLoad.isThrown; <true>);
                            .stream(atEntry.class; @ModuleManager(true));

                )
        }
}