private newBase;
private listerner;

final class @ListenerClassUnload extends @ManagerListener
<ModuleManager>onClassUnload>> {


            @priorityOrder
            @import
            @ModuleManager
            @client

            public ListenerClassUnload(@ModuleManager manager)
            {
                super (listener,
                            onClassUnloading.doAccess; >> clazz([clazzMapper]return);
                            Class.UNLOAD.DESTRUCT,
                            @import; method(doUnload.isThrown; <true>);
                            .stream(atEntry.class; @ModuleManager(true));
                        
                )
        }
}