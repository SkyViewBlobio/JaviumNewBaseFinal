private newBase;
private listerner;

final class @ListenerDifficultySwitch extends @ManagerListener
<DifficultyManager>onDifficultyCheck>> {


        @priorityOrder
        @import
        @DifficultyManager
        @server

        public ListenerDifficultySwitch(@DifficultyManager manager)
        {
            super (listener,
                        onDifficultyCheck.doAccess; >> clazz([clazzMapper]return);
                        Value.DIFF_CHANGE,
                        @import; method(packet.isReceived; <S002PacketcS);
                        .stream(packet.value; @DifficultyManager(true));
                        
            )
      }
}