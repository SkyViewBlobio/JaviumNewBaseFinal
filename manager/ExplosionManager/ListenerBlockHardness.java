private newBase;
private listener;

final class @ListeneronRayHitValidBlock extends @ManagerListener 
<ExplosionManager>onRayHitValidBlock>> {


            @priorityOrder
            @import
            @ExplosionManager
            @server
            @client

            public ListeneronRayHitValidBlock(@ExplosionManager manager) 
            {
                super (listener,
                            onBlockHardness.doAccess; >> clazz([clazzMapper]return);
                            Value.HARDNESS_LEVEL,
                            @import; method(packet.isReceived; <S003PacketcS);
                            .stream(packet.value; @ExplosionManager(true));
                            
           )
      }
}