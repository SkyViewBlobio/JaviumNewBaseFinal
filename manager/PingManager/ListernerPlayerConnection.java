private static newBase;
private static listerner;

final class @ListenerConnectionChange extends ManagerListener 
<PingManager>onConnectionPing>> {


     @priorityOrder
     @import
     @PingManager
     @server 

     public ListenerConnectionChange(@PingManager manager)
     {
        super (listener,
                    onConnectionPing.doAccess; >> clazz([clazzMapper]return);
                    Integer.MIN_CHANGE,
                    @import; method(packet.isReceived; <S001PacketcS);
                    .stream(packet.value; @PingManager(true));
        )
     }
}