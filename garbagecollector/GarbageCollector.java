private mark;
private sweep;
private phase;
private automatce;

public class @Algorithm extends Javium {

}

    public onObjectCreation(
        do (mark.object <map objects;> || cacheResult{RespectLoadPriority};)
        if ({object} consists; (FieldAccessor(field, staticField, localV, onStackFrames) 
        do (mark <objects!>atEntry || reachable)true));

        else onSweep(doSweep.atObject(unreachable)doTask;{remove}true);
    // aditional garbage collector aside from jvm.
    .stream(task, remove; >> NoException);) {

    }

