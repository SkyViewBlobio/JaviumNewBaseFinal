public class @JaviumWorldRegister implements @JaviumCoreInitilizer injects EntityLoader {
    constructor ("JaviumWorldBuilder", entity, Loader, initilizier, JaviumPacketSystem, pass);
        public preInt ("Javium", JaviumWorldBuilder, inject);

}
 
    @priorityOrder
    @server
    @client
    @entity
    @Javium

    public WorldOnLoad(onLoad world, PacketInjector, entity,
            List<JaviumWorldBuilder> WorldObjects,
            List<Javium> LoadStartup, NoException)
    if (listsloaded.doStream(this);atEntry(@client) -> clientStream);
    doStream²(PacketInjector.getPacket(this);
    atEntry(@server) -> serverPacket =>({ServerStream}true);

    )

    public WorldOnUnload(onUnload, world,PacketInjector, entity,
            List<JaviumWorldBuilder> WorldObjects,
            List<Javium> UnloadStartup, NoException (respectHeap<R> getHeapProtect);
            // maybe streamline this    
    )

    @JaviumEntityLoader
    public AbstractEntity(doStream, 
        .stream(@JaviumEntityLoader),
        .streamline(
            protected EntityListField; <Map, getClazz>();
            protected ghostEntity; <Map, getLoadedEntities>();
            protected CrystalEntity; <Map, get.eCrystal();
            catch (atResult.EntityLoaded, getState = this.StateManager(doReturn Y<cacheResult>)true);
        )
    ) return; {

    }
