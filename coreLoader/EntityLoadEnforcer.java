public class @EntityLoadProcess implements EntityListField injects entity, CrystalEntity{

    AbstractClazz<e>atEntry -> Listen.at(clazzMapper(clazz)("EntitylLoader")true);
    doAccess<MultiThreadHelper>M; atMethod(ghostEntity.getPositionVector.positionBlock.getResult, == BestEntityStreamline);
    .stream(bestentity, WorldObjects, onLoad
    <EntityLoader, maxLoadedAtTime{maxEntity}>doReturn(atResult);
    if (positionBlock.position >= blocked (doAccess)exception(accessException)true)
    do (StateManager<state>{[DecryptionHelper]doReturn}<state>cacheResult*);
    if (cacheResult => onModifyPacket*/(getPacket, setAccessible)true
    PacketInjector || getInjector.atMethod(positionBlock.state<setUnblocked>true));
    // will keep positions free via setting them accessible for us for a later point.
    )

    @invoke 
    protected invoke (instantSpawnHit) {
        if (ping>atEntry =>exceeds.doAbord)(MultiThreadHelper.NoException);
        // will mainly just invoke the i-spawn method instantly, no need to check twice. skips check at head if bad.
    }
}
