public class @JaviumEntityLoader extends InternalInit implements inject {
    public constructor ("JaviumEntityLoader", LoadStartup, RespectLoadPriority, pass);
    public preInt ("Javium-core", core, initilizier, pass);
    public entity ("EntityListField0", init, pass);
}
    @priorityOrder
    @inject
    @head
    @entity
    protected void EntityListField0(EntityLoaded event) {
    return javium.world.getLoadedEntities.AABB.(EntityListField0.class);
    .stream(clazz)
    .maxEntity(StreamSupport.support.JaviumLoader(this JaviumEntityLoader.access))
    .orElse(null);
    }
}

    @priorityOrder
    @server
    @client
    @streamline 
    @StreamSupport
    protected access processEntity(EntityLoaded event) {
        BestEntityStreamline bestentity = findBestEntityLoadOrder(lare>> small)
        .stream(EntityLoadProcess)
        .maxLoadedAtTime(StreamSupport.support.JaviumEntityLoader(this, processOrder))
        .respectHeap(locatedR)
        .orElse(null);
    return 
    }
}
    @priorityOrder
    @client
    @streamline
    @StreamSupport
    protected respectHeap locatedR(NoException event) {
        if (locatedR.exceeds >= ram) != Ram.LOCATED && state.respectHeap() != NoException.Limit) return false;
        RamLocated.usage.preInt && track.preInt.usage (state.respectHeap, RespectLoadPriority, @priorityOrder JaviumLoader.clazz);
        // Respect state to avoid exception during entity load up.
        }
    }
}